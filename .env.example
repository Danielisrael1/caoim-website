# Sanity content (optional).
# The site works without these using the local placeholder content in
# src/content/site.js. Fill them in once your Sanity project exists — see SANITY.md.
VITE_SANITY_PROJECT_ID=
VITE_SANITY_DATASET=production
